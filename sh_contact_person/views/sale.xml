<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sh_contact_person_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="contact_person_id" domain="[('parent_id','=', partner_id )]" />
            </field>
        </field>
    </record>
    <!-- sale order tree view -->
    <record id="sh_contact_person_tree" model="ir.ui.view">
        <field name="name">sale.order.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree_with_onboarding" />
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="contact_person_id" />
            </field>
        </field>
    </record>
    <!-- sale order search view -->
    <record id="sh_contact_person_search" model="ir.ui.view">
        <field name="name">sale.order.search</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="contact_person_id" string="Contact Person" />
            </xpath>
            <xpath expr="//filter[@name='customer']" position="after">
                <filter string="Contact Person" name="contact_person" context="{'group_by' : 'contact_person_id'}" />
            </xpath>
        </field>
    </record>
</odoo>
